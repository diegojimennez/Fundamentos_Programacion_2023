#include <stdio.h>

int main()
{

    /*Declarar las variables*/

    int n1, n2, n3, n4, n5;

    int max, min;

    /*Solicitar los números*/

    printf("\nDame 5 numeros separados por comas: ");
    scanf("%d, %d, %d, %d, %d", &n1, &n2, &n3, &n4, &n5);
    max = n1;
    min = n1;

    max =
}